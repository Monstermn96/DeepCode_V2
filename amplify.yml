applications:
  - appRoot: .
    backend:
      phases:
        preBuild:
          commands:
            - if [ "$FORCE_CLEANUP" = "true" ]; then bash ./scripts/cleanup-predeploy.sh; fi
        build:
          commands:
            - npm ci
            - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - if [ "$AUTO_UPDATE_AUTH" = "true" ]; then node ./scripts/update-auth-config.js; fi
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    environment:
      variables:
        FORCE_CLEANUP: "false"
        AUTO_UPDATE_AUTH: "true"