applications:
  - appRoot: .
    backend:
      phases:
        preBuild:
          commands:
            - nvm install 20
            - nvm use 20
            - npm install @aws-sdk/client-cognito-identity-provider @aws-sdk/client-amplify @aws-amplify/backend@1.14.1
            - npm list @aws-amplify/backend
        build:
          commands:
            - npm install
    frontend:
      phases:
        preBuild:
          commands:
            - npm install 20
            - nvm use 20
            - npm install @aws-sdk/client-cognito-identity-provider @aws-sdk/client-amplify @aws-amplify/backend@1.14.1
            - npm list @aws-amplify/backend
        build:
          commands:
            - if [ "$FIRST_DEPLOY" = "true" ]; then node ./scripts/update-auth-config.js; fi
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
